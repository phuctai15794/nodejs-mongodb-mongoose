<div class="row">
    <div class="col-3">
        <div class="text-center">
            <button class="btn btn-primary text-capitalize font-weight-bold mb-3">Học ngay</button>
            <div class="alert alert-info">{{ courseDetail.level }}</div>
        </div>
    </div>
    <div class="col-9">
        <h2 class="mb-3">{{ courseDetail.name }}</h2>
        <div class="mb-3"><iframe width="100%" height="450" src="https://www.youtube.com/embed/{{ courseDetail.videoID }}" title="{{ courseDetail.name }}" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe></div>
        <p>{{ courseDetail.description }}</p>
    </div>
</div>